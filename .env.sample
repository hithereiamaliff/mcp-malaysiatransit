# Malaysia Transit Middleware API URL
# Default: http://localhost:3000 (for local development)
# Production: https://your-middleware-url.com
MIDDLEWARE_URL=http://localhost:3000

# Google Maps API Key (optional)
# Used for more accurate geocoding. If not provided, falls back to Nominatim (free but less accurate)
# Get your API key from: https://console.cloud.google.com/
GOOGLE_MAPS_API_KEY=

# Internal auth token for middleware API bypass (required for production)
# Must match INTERNAL_APP_AUTH_TOKEN in the middleware's .env
INTERNAL_AUTH_TOKEN=
