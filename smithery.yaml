version: "1.0"
runtime: typescript
config:
  middlewareUrl:
    type: string
    description: "Malaysia Transit Middleware API URL"
    default: "https://malaysiatransit.techmavie.digital"
  googleMapsApiKey:
    type: string
    description: "Google Maps API Key for geocoding (optional, falls back to Nominatim if not provided)"
    default: "AIzaSyClzCRmcl-uWbjuN8VMboVExc0Hi-IyqAI"
